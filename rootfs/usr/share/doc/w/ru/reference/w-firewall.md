---
title: w-firewall
section: reference
order: 0
summary: Переключение зоны файрвола между домашней и публичной (firewalld).
---

`w-firewall` — Переключение зоны файрвола между домашней и публичной (firewalld).

## Использование

```
Использование: w-firewall <команда>

Info: Переключение зоны файрвола между домашней и публичной (firewalld).

Команды:
  status        Зона по умолчанию, активные зоны и набор правил зоны по умолчанию
  home          Зона по умолчанию — 'home'  (база W: входящие ssh + mdns)
  public        Зона по умолчанию — 'public' (чужие сети; только входящий ssh)
  help          Показать эту справку

Коды выхода:
  0  Успех
  1  Ошибка выполнения
  2  Ошибка вызова

Примеры:
  w-firewall status
  sudo w-firewall public
```

Эта страница генерируется из собственной справки команды (w-docs-refgen). Живая
версия того же текста на машине с W: `w-firewall help`.
