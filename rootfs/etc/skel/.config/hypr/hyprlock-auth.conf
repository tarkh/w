# W Linux — GENERATED by w-fingerprint (lock-sensor mode: native). Do NOT edit;
# change the mode with 'w-fingerprint lock-sensor mode <native|wake>' or in the
# Hub (Input → Fingerprint). Sourced last by hyprlock.conf. See package-hyprlock.md.
