-- W Linux — monitor layout. Managed by `w-monitor` (and the W Hub Displays panel);
-- hand-edits are overwritten. Data module require()d by hyprland.lua.
--
-- primary : output whose bar is the main bar (W concept; Hyprland has no "primary").
-- outputs : ordered rules; declaration order drives auto-* placement chaining.
return {
  primary = "",
  outputs = {
  },
}
