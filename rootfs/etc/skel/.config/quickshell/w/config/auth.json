{
  "_comment": "W auth prompt behaviour (user-owned; w-style/w-theme never touch it). border: number or geometry token. dim: backdrop opacity (no blur). cardWidth: px.",
  "border": "border",
  "dim": 0.45,
  "cardWidth": 460
}
