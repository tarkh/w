# GENERATED by w-i18n-gen from i18n/installer.json — do not edit.
# Language: en.
#
# W Linux installer strings (bash + dialog, running off the ISO). Format of the
# generated file: key=value, the value being everything after the first '=' —
# no quotes, and it is parsed line-wise, never sourced. dialog interprets \n as
# a newline and \Z? as a colour code (--colors). Add a language by adding its
# code to every entry here; the wizard's language menu discovers the generated
# <code>.conf on its own and labels it with that language's own _lang_name.
# Keys starting with '_' are per-language metadata, not translations, and may
# exist for one language only.
_lang_name=English

# ── Generic UI ───────────────────────────────────────────────────────────────
back=Back
next=Next
quit=Quit
yes=Yes
no=No
ok=OK
please_wait=Please wait…
installing=Installing
installing_packs=Installing software bundles
error=Error

# ── Language / welcome ───────────────────────────────────────────────────────
lang_title=Language
lang_prompt=Select the installer language:
welcome_title=Welcome
welcome_body=\Zb  W Linux Installer\Zn  —  Arch-based\n\n  This wizard guides you through installing\n  a minimal, encrypted-capable base system.

# ── Wizard steps ─────────────────────────────────────────────────────────────
s_net_title=Wi-Fi
s_net_prompt=Select a Wi-Fi network to connect (needed to download packages now and after reboot), or skip if you use a wired connection:
s_keymap_title=Keyboard Layout
s_keymap_prompt=Select the console keyboard layout.\n\nThis layout is used to type the disk passphrase at every boot — pick the one that matches your physical keyboard.
s_locale_title=System Locale
s_locale_prompt=Select the system language/locale (LANG). English is always kept as a fallback.
s_computer_title=Computer Type
s_computer_prompt=Is this a laptop or a desktop?\n\nIt selects the power preset: a laptop gets battery-aware idle/sleep timers, lid actions and auto power-profile switching; a desktop gets gentler defaults and no battery controls. Auto-detected below — change it with 'w-power mode' later.
s_computer_laptop=Laptop
s_computer_desktop=Desktop
s_mode_title=Update Channel
s_mode_prompt=How should this system receive W updates?\n\nEdge = track the W git repository and pull updates directly, with a snapshot taken before each one. This is the only channel during the beta.\n\nStable (a signed package from the W repo) is not available yet.
s_mode_stable=Stable
s_mode_edge=Edge
s_edge_title=Edge Repository
s_edge_prompt=Pre-filled with the official W repository — press Enter to accept, or replace it with your own fork (a private one needs credentials in the URL). It is cloned now to check access.
s_disk_title=Select Disk
s_disk_prompt=Choose the installation target.\n\nALL DATA on the selected disk will be ERASED.
s_encrypt_title=Disk Encryption
s_encrypt_prompt=Encrypt the disk with LUKS2 (Argon2id) and boot via Limine with snapshot support?\n\nNo  = standard install (GRUB, unencrypted).\nYes = encrypted install (LUKS2 + Limine).
s_luks_title=Disk Passphrase
s_luks_prompt=Enter the LUKS disk passphrase (asked at every boot unless a TPM2 unlocks it):
s_host_title=Hostname
s_host_prompt=Enter the system hostname:
s_tz_title=Timezone
s_tz_prompt=Search by city or region (e.g. Moscow, New_York) — leave empty to browse all:
s_tz_pick_prompt=Select your timezone:
s_root_title=Root Password
s_root_prompt=Enter the root password:
s_user_title=Create User
s_user_prompt=Enter the username for the primary user:
s_userpass_title=User Password
s_userpass_prompt=Enter the password for the primary user:
s_packs_title=Software Bundles
s_packs_prompt=Select optional software bundles to install on first boot.\n\nThese are add-ons by direction (containers, graphics, …) on top of the base system. Toggle with Space; installed automatically after the base setup. Leave all unchecked to install none.
p_containers=Podman rootless containers + Docker CLI + lazydocker
p_dev=Development environment: Zed editor, git tooling (delta, lazygit), mise
p_flatpak=Sandboxed third-party apps: Flatpak + Flathub, the Bazaar store, Flatseal
p_office=Office and documents: LibreOffice, PDF tools (pdfarranger, xournalpp), Obsidian notes
p_telegram=Telegram Desktop messenger, themed by W
p_localsend=LocalSend: AirDrop-like file transfer to phones/laptops, themed by W
p_graphics=Graphics and design: GIMP, Inkscape, Krita, G'MIC filters, AI assistant bridges
p_virt=Desktop virtualization: libvirt + QEMU/KVM, virt-manager, GNOME Boxes
p_gaming=Gaming: Steam, Proton tooling, Lutris and Heroic launchers, MangoHud overlay

# ── Validation / passwords ───────────────────────────────────────────────────
pass_confirm=Confirm password:
pass_mismatch=Passwords do not match or are empty. Try again.
err_invalid=Invalid value. Try again.
err_timezone=Unknown timezone. Use a valid zoneinfo path (e.g. Europe/Moscow).
err_tz_nomatch=No timezones match your search. Try a different city or region.
err_offline=No network connection. Connect a cable or Wi-Fi, then retry — the installer needs internet to download packages.
edge_cloning=Cloning the repository to verify access…
err_edge_empty=Enter a repository URL, or go back and choose the Stable channel.
err_edge_git=git is unavailable and could not be installed. Check the network and retry.
err_edge_clone=Clone failed. Check the URL, credentials and network, then retry.
err_edge_notw=This does not look like a W repository (scripts/apply.sh not found).

# ── Wi-Fi ────────────────────────────────────────────────────────────────────
net_scanning=Scanning for Wi-Fi networks…
net_skip=✕  Skip (wired / no Wi-Fi)
net_password=Enter the password for network:
net_connecting=Connecting to
net_connect_fail=Connection failed. Check the password and try again.
net_none=No Wi-Fi networks found. Rescan, or skip.

# ── Review hub ───────────────────────────────────────────────────────────────
review_title=Review
review_prompt=Review your choices. Select an entry to change it, or proceed.
review_proceed=▸  Proceed with installation

# ── Confirm / summary ────────────────────────────────────────────────────────
summary_title=Installation Summary
confirm_title=Confirm Installation
confirm_prompt=Proceed with installation?\n\nThis will ERASE all data on the target disk.
quit_title=Quit
quit_prompt=Quit the installer? No changes have been written to disk yet.

# ── Work phases ──────────────────────────────────────────────────────────────
phase_disk=Preparing disk
phase_base=Installing base system
phase_config=Configuring system
phase_boot=Installing bootloader
phase_services=Enabling services

# ── Done ─────────────────────────────────────────────────────────────────────
done_title=Installation complete.
done_reboot=Remove the installation media, then reboot.\n  The rest of the setup finishes automatically on first boot.
done_luks_notice=Your disk is encrypted: on the next boot you will be asked to enter\n  the decryption passphrase you set during installation before W can start.
done_secureboot_notice=Secure Boot and TPM2 disk auto-unlock (booting without typing the\n  passphrase) are turned on from Hub → Security once installed, in that order.
reboot=Reboot

# ── Firstboot (Session 2) ────────────────────────────────────────────────────
firstboot_title=Configuration complete.
firstboot_done=Rebooting automatically in a few seconds.\n  Press Reboot to continue now.
firstboot_failed=First-boot configuration failed. Check the log:
