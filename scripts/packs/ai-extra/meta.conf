# W-Packs bundle: ai-extra — advanced AI stack (local models, semantic memory
# groundwork, better web search) for the built-in system AI.
#
# Shell-sourced by w-pack (meta_field) in an isolated subshell — every multi-word
# value MUST be quoted, or the line parses as a command. See packs.md /
# ai-integration.md.

NAME="ai-extra"
DESC="Advanced AI stack: local models (Ollama), semantic memory, better web search"
SCOPE="mixed"          # packages (root) + user-scope tools (uv, ollama models)
DEPS=""                # no prerequisite bundles
WEIGHT="heavy"         # GPU driver variants + a multi-hundred-MB model pull
INSTALLER="off"        # userspace-only: never offered in the TUI installer checklist
