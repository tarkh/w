# W-Packs bundle: containers — Podman rootless + Docker-compat + lazydocker.
#
# Shell-sourced by w-pack (meta_field) and the installer TUI (packs_list) in an
# isolated subshell — every multi-word value MUST be quoted, or the line parses as
# a command. See packs.md / pack-containers.md.

NAME="containers"
DESC="Podman rootless containers + Docker CLI + lazydocker"
SCOPE="mixed"          # packages (root) + config (user rootless / system dns)
DEPS=""                # no prerequisite bundles
WEIGHT="medium"        # install cost hint (reserved for a future deferred mode)
